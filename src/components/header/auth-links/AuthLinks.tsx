import { Button } from "@mui/material";
import { FC } from "react";

const AuthLinks: FC = () => {
  return (
    <div>
      <Button>Login</Button>
      <Button>Register</Button>
    </div>
  );
};

export default AuthLinks;
